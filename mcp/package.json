{
  "name": "course-plus-mcp",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local MCP server for Course+ — lets Claude Desktop read + write your Course+ data (cp_* tables, per-user RLS).",
  "bin": { "course-plus-mcp": "server.js" },
  "scripts": {
    "login": "node login.mjs",
    "start": "node server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "@supabase/supabase-js": "^2.106.1",
    "zod": "^3.23.8"
  }
}
