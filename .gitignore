node_modules
dist
.env
.DS_Store
*.local

.claude/worktrees/

# local design reference (not shipped)
_ref/

# MCP server — local deps + saved session (refresh token, never commit)
mcp/node_modules
mcp/.session.json

# Supabase CLI temp
supabase/.temp/
