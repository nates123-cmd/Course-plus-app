node_modules
dist
.env
.DS_Store
*.local

.claude/worktrees/

# local design reference (not shipped)
_ref/

# MCP server — local deps + saved session (refresh token, never commit)
mcp/node_modules
mcp/.session.json

# Supabase CLI temp — anywhere, not just the project root: running the CLI
# from inside supabase/functions writes a nested copy.
**/.temp/

# Tauri desktop build artifacts
src-tauri/target/
src-tauri/gen/
